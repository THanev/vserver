// the [dist] folder is required for loading resources inside of it
// but [dist] can't be matched by gulp
// either remove [dist] and place every single new resources in resources folder
// or find a regex to match [dist] with gulp